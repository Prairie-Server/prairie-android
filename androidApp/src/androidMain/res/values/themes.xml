<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base XML theme for the splash / system integration layer.
        Compose applies SiloTheme at runtime for all in-app theming.
    -->
    <style name="Theme.Silo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/dark_surface</item>
        <item name="android:windowBackground">@color/dark_background</item>
    </style>
</resources>
