Prefer shared/ and android-shared/ for cross-surface logic. TV uses Compose for TV / leanback; keep Fire TV launcher/banner safe-zone constraints if touching icons.

Deep links use prairie://. HTTP device headers are X-Prairie-*.
